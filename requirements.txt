# Core scientific stack
numpy>=1.24
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Deep learning / CEBRA
torch>=2.0
cebra>=0.4.0

# Neuroimaging
nibabel>=5.0
nilearn>=0.10

# Empirical identifiability / consistency ensembles
# (ituna is not on PyPI -- install from GitHub, see README.md)
# pip install git+https://github.com/dynamical-inference/ituna.git

# Notebooks (optional, only needed if you want to explore interactively)
jupyter>=1.0
ipykernel>=6.25
